# Copy to .env and fill in your values.

# Path to your Google OAuth client JSON (download from Google Cloud Console).
GMAIL_CLIENT_SECRET=client_secret.json

# Local Ollama endpoint and model.
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL=mistral

# Optional: words per chunk when processing long inputs.
CHUNK_SIZE=300
